import * as React from 'react';

export interface IAppProps {
}

export function AddToDo(props: IAppProps) {
    return (
        <div className="Add-Todo-Border">
            Add ToDo
        </div>
    );
}
